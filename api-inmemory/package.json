{
  "name": "api4",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "echo 'build v2 api' && tsc",
    "watch": "tsc -w",
    "prestart": "rm -rf dist && npm run build",
    "start": "echo 'run v2 api' && func start --typescript --verbose",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.11.2"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.6.0",
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  },
  "main": "dist/src/functions/*.js"
}
